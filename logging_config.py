import logging

# Set up logging configuration
logging.basicConfig(
    level=logging.INFO,  # Change from DEBUG to INFO to reduce log verbosity
    format="%(asctime)s - %(levelname)s - %(message)s",
)

# existing code...
